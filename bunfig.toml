# Bun configuration file
# https://bun.sh/docs/runtime/bunfig

[install]
# Use exact versions instead of ranges
exact = false

# Install peer dependencies automatically
peer = true

[test]
# Use test files with .test.ts or .spec.ts
