<Page
    x:Class="LaddersAndSlidesW8UI.Pages.EntryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LaddersAndSlidesW8UI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        
        <Canvas Grid.Row="0" Grid.RowSpan="2" Name="MainScreen">
            <Canvas.Background>
                <ImageBrush ImageSource="../../Assets/SplashScreen.png"></ImageBrush>
            </Canvas.Background>
        </Canvas>
        
        <Button Grid.Row="1" Name="PlayGame" Margin="60,75,60,10" HorizontalAlignment="Center" VerticalAlignment="Bottom" 
                FontFamily="Andy" FontSize="96" FontStyle="Italic" FontWeight="Bold" Background="Transparent" 
                Foreground="#FF6A197C" Width="800" Height="200" BorderBrush="{x:Null}" BorderThickness="0" IsDoubleTapEnabled="False" IsRightTapEnabled="False" Click="PlayGame_OnClick">Play Game!
        </Button>
    </Grid>
</Page>
